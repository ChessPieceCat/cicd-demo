services:
  cicd-demo:
    image: ghcr.io/chesspiececat/cicd-demo:latest
    container_name: cicd-demo
    restart: unless-stopped

    ports:
      - "8082:80"

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost"]
      interval: 30s
      timeout: 5s
      retries: 3
